pandas>=2.0.0
matplotlib>=3.8.0
pyyaml>=6.0
psutil>=5.9.0
# Optional GPU sampling (self-host / ECS GPU host)
nvidia-ml-py>=12.0.0
# LLMPerf: clone to vendor/llmperf; run via PYTHONPATH=src (see run_one.sh).
# Do not pip install LLMPerf itself (pins Python <3.11). Pull its runtime deps here.
# boto3 is imported at module load via sagemaker_client even for --llm-api openai.
ray>=2.9.0
transformers>=4.36.0
openai>=1.0.0
tqdm>=4.66.0
num2words>=0.5.13
boto3>=1.28.0
requests>=2.31.0
pydantic>=2.0.0
